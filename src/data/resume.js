// Bump this date whenever resume.pdf is replaced, so the URL changes and
// browsers/CDNs treat it as a new resource instead of serving a stale cache.
export const RESUME_URL = "/resume.pdf?v=2026-07-08";
